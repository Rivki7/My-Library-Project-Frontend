import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#008080',
    },
    secondary: {
      main: '#ffffff',
    },
    accent: {
      main: '#ff6f61',
    },
    background: {
      default: '#f5f5f5',
    },
    text: {
      primary: '#000000',
      secondary: '#ffffff',
    },
  },
});

export default theme;
