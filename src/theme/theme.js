import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#008080',
    },
    secondary: {
      main: '#ffffff',
    },
    accent: {
      main: '#ff6f61',
    },
    background: {
      default: '#f5f5f5',
    },
    text: {
      primary: '#323232',
    },
  },
});

export default theme;
